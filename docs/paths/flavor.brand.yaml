post:
  operationId: PostFlavorBrand
  tags:
    - flavor
  summary: Post flavor brand api
  requestBody:
    description: Flavor brandを登録するapi
    content:
      application/json:
        schema:
          $ref: "../schemas/PostFlavorBrandRequest.yaml"
    required: true
  responses:
    "200":
      description: Successful operation
    "400":
      description: Invalid request
